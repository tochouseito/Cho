#include "PrecompiledHeader.h"
#include "Enemy.h"

void Enemy::Init(SceneManager* sceneManager, ComponentManager* compManager, InputManager* inputManager)
{
	sceneManager_ = sceneManager;
	compManager_ = compManager;
	input = inputManager;

	// コンポーネントの取得

}
